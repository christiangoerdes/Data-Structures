#pragma once

#include "listNode.hpp"

/**
 * @brief A class representing an iterator for a linked list.
 *
 * This class provides methods for iterating over a linked list and accessing its elements.
 *
 * @tparam T The type of elements stored in the linked list.
 */
template <typename T>
class ListIterator {

};
